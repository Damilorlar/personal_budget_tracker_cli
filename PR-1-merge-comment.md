Merging PR #1: Accept PR changes and update README

I resolved merge conflicts by accepting the changes from the PR branch (Doc) and kept the updated user flow.

Next steps I recommend:
- Run the test suite (pytest) and linting (ruff) locally or via CI.
- If you want, I can add a CI workflow to run tests and lint automatically (I need write access to add it).

If anything looks off after merging, I can open follow-up PRs to fix issues.
